# Barcode Scanning Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** Optional barcode per item + scan-to-cart on the register (scanner types code + Enter → adds the matching item). Client-side match against the already-loaded sale items.

---

## File Structure
- **Modify:** `convex/schema.ts`, `convex/menu/items.ts`, `src/components/menu/item-edit-form.tsx`, `src/components/sale/menu-pane.tsx`, `src/components/sale/sale-screen.tsx`, `tests/convex/menu-items.test.ts`.
- **i18n:** `src/locales/{id,en}/messages.po`.

---

### Task 1: Backend — barcode field + dup-check (TDD)
**Files:** modify `convex/schema.ts`, `convex/menu/items.ts`; test `tests/convex/menu-items.test.ts`.

READ: `convex/menu/items.ts` — `menuItemDoc` (~line 7), `create` (~151), `update` (~185), `itemForSale` (~312), `itemDetail` (~75), `getById`/`listForSale`; how a duplicate-name check is done (mirror for barcode); `convex/schema.ts` `menuItems`; the existing create/update tests in `tests/convex/menu-items.test.ts`.

- [ ] **Step 1: schema** — `menuItems`: add `barcode: v.optional(v.string())` + `.index('by_cafe_barcode', ['cafeId', 'barcode'])`.
- [ ] **Step 2: FAILING tests** (append to `tests/convex/menu-items.test.ts`):
  - `create` with `barcode: '8991234567890'` → `getById` returns it; `listForSale` item carries it.
  - a second `create`/`update` with the SAME barcode in the cafe → rejects (`/barcode/i`); a second cafe (`setup` with other email) with the same barcode is allowed.
  - `update` clearing the barcode (pass `barcode: ''` or omit) → item has no barcode.
  Run → confirm FAIL.
- [ ] **Step 3: implement** — `menuItemDoc` + `itemForSale` (its `item` object) + `itemDetail`: add `barcode: v.optional(v.string())`. `create`/`update` args add `barcode: v.optional(v.string())`; compute `const bc = barcode?.trim();`; when `bc` is non-empty, query `by_cafe_barcode` (`eq('cafeId',cafeId).eq('barcode',bc)`) and throw `'Barcode sudah dipakai item lain.'` if a different non-archived item matches (exclude the current `id` on update). Insert/patch `...(bc ? { barcode: bc } : {})` on create; on update `patch({ barcode: bc || undefined })` (undefined clears it under exactOptionalPropertyTypes — Convex patch clears with undefined). Confirm `listForSale`/`getById` carry `barcode` (it's in `menuItemDoc`/`itemForSale.item`).
- [ ] **Step 4: tests + typecheck + commit**
  `pnpm test tests/convex/menu-items.test.ts` + full PASS; `pnpm typecheck` PASS. Commit:
  `git add convex/schema.ts convex/menu/items.ts tests/convex/menu-items.test.ts && git commit -m "feat(menu): item barcode field + unique-per-cafe guard"`
  > Do NOT run codegen.

---

### Task 2: Frontend — admin barcode input + scan-to-cart
**Files:** modify `src/components/menu/item-edit-form.tsx`, `src/components/sale/menu-pane.tsx`, `src/components/sale/sale-screen.tsx`.

READ: `src/components/menu/item-edit-form.tsx` (the name/price fields + the create/update calls + how `getById` data is passed in), `src/components/sale/menu-pane.tsx` (the pane layout + `ItemForSale` type + the category controls), `src/components/sale/sale-screen.tsx` (`items` from `listForSale`, `onItemTap`, `toast`/`useLingui`).

- [ ] **Step 1: admin** — `item-edit-form.tsx`: add a **Barcode** `Input` (optional) into the form state, seeded from the item's `barcode`; include `barcode: barcode.trim() || undefined` in the `create`/`update` mutation args. A small hint label.
- [ ] **Step 2: ItemForSale type** — `menu-pane.tsx`: add `barcode?: string` to the `ItemForSale` `item` shape (it flows from `listForSale`).
- [ ] **Step 3: scan input** — `menu-pane.tsx`: add an `onScan?: (code: string) => void` prop and render a scan `<form>` at the top of the pane: an `Input` (`placeholder={t\`Scan / ketik barcode…\`}`, `inputMode="numeric"`, a `ScanLine` lucide icon, `autoFocus`, controlled local state). On submit: `if (code.trim()) onScan?.(code.trim());` then clear the input + refocus.
- [ ] **Step 4: sale-screen** — pass `onScan` to `<MenuPane>`:
  ```ts
  function onScan(code: string) {
    const row = items.find((r) => r.item.barcode === code);
    if (row) onItemTap(row);
    else toast.error(t\`Barcode tidak ditemukan.\`);
  }
  ```
  (`items` + `onItemTap` already exist; `toast`/`t` import if needed.)
- [ ] **Step 5:** `pnpm typecheck` + `pnpm test` PASS. Commit:
  `git add src/components/menu/item-edit-form.tsx src/components/sale/menu-pane.tsx src/components/sale/sale-screen.tsx && git commit -m "feat(sale): barcode admin field + scan-to-cart"`

---

### Task 3: i18n
New: `Barcode`, `Scan / ketik barcode…`, `Barcode tidak ditemukan.` (+ the server `Barcode sudah dipakai item lain.` is off-catalog/thrown).
- [ ] `pnpm lingui:extract`; fill `en` (`Barcode`, `Scan / type barcode…`, `Barcode not found.`) + any other new empties; `pnpm lingui:compile` → en 0 missing. Commit `src/locales`.

---

### Task 4: Final verification
- [ ] `pnpm typecheck` PASS; `pnpm test` PASS; `pnpm lingui:compile` en 0 missing; `git status` clean (no route change → no routeTree).
- [ ] **Manual sanity:** set a barcode on an item; on `/sale` scanning/typing that code + Enter adds it to the cart (a variant/modifier item opens the picker); an unknown code toasts "not found"; a duplicate barcode is rejected in the item form.

---

## Self-Review
**Spec coverage:** schema barcode + index (T1); doc/create/update/itemForSale/itemDetail + dup guard (T1); admin input (T2); ItemForSale type + scan input + sale-screen match→onItemTap (T2); tests store/dup/cross-cafe/clear (T1); i18n (T3). ✓
**Placeholder scan:** test seeding "copy from menu-items tests". Else spec code.
**Type consistency:** `barcode?: string` on `menuItemDoc`/`itemForSale.item`/`ItemForSale` flows to the client match; `create`/`update` accept `barcode?`. Scan match is client-side against `listForSale` `items`; `onItemTap` reused. ✓
