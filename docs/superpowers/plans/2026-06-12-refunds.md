# Refunds / Returns Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`). Money path (stock + cash + loyalty + gift card) → TDD + adversarial review.

**Goal:** Full + partial refunds to an original tender — restock returned items, pro-rate loyalty, gift-card/cash reversal, and reflect refunds in the money-truth reports. QRIS record-only.

---

## File Structure
- **Create:** `convex/refunds.ts`, `convex/lib/refund.ts`, `tests/convex/refunds.test.ts`, `src/components/sale/refund-dialog.tsx`.
- **Modify:** `convex/schema.ts` (`refunds` table + `orders.refundedIDR`), `convex/orders.ts` (`refundInfo` query + expose `refundedIDR` on `getById`), `convex/reports.ts` (`profitLoss`, `overview`), `convex/dashboard.ts` (kpis), `convex/_generated/api.d.ts`, `src/components/sale/receipt-preview.tsx`, `src/routes/_pos/reports/profit-loss.tsx`.
- **i18n:** `src/locales/{id,en}/messages.po`.

---

### Task 1: Backend — refunds table + pure helpers + `refunds.create` + `refundInfo` (TDD)
**Files:** create `convex/refunds.ts`, `convex/lib/refund.ts`, `tests/convex/refunds.test.ts`; modify `convex/schema.ts`, `convex/orders.ts`, `convex/_generated/api.d.ts`.

READ: `convex/lib/sale.ts` `reverseSettledSale` (lines ~552–635 — the reversal mechanics to mirror: inventory restock `reason:'adjustment'`/`reasonLabel`, loyalty floor-at-0, gift-card `giftCardTransactions{refund}`), `settleSale` (inventory `reason:'sale'` deduction), `buildOrder` idempotency (`by_cafe_clientId`); `convex/orders.ts` `voidSale` (auth/guards) + `getById`; `convex/cashMovements.ts` (`record`/insert shape + how the open shift is found); `convex/shifts.ts` (open-shift lookup by cafe); `convex/lib/payment.ts` `methodTotals` (order tenders); `convex/lib/inventory.ts` `currentStockQty`; `convex/lib/auth.ts`; `tests/convex/orders.test.ts` (setup: owner, open shift, a paid order via `createCashSale`/`createSplitSale`, a recipe-backed item so stock moves).

- [ ] **Step 1: schema** — add the `refunds` table (spec shape) with `by_cafe_clientId` + `by_order` + `by_cafe_at` + `by_shift` indexes; add `refundedIDR: v.optional(v.number())` to `orders`.
- [ ] **Step 2: pure helpers `convex/lib/refund.ts`** (no ctx — unit-testable):
  - `unitRefundIDR(unitPriceIDR, orderTotalIDR, orderSubtotalIDR): number` = `Math.round(unitPriceIDR * orderTotalIDR / orderSubtotalIDR)` (subtotal>0 guaranteed by ≥1 line).
  - `validateRefundLines(orderLines, alreadyRefundedQtyByIndex, requested): { lines: [{lineIndex, nameSnapshot, qty, lineRefundIDR}], amountIDR, fullyRefundsOrder }` — throws on out-of-range index / qty≤0 / non-integer / `qty > remaining` (`'Melebihi jumlah yang bisa direfund.'`) / empty (`'Pilih item untuk direfund.'`); computes `lineRefundIDR` via `unitRefundIDR`; `fullyRefundsOrder` true iff after this every line's refundedQty===qty.
- [ ] **Step 3: FAILING tests** (`tests/convex/refunds.test.ts` + a small `tests/convex/refund-math.test.ts` for the pure helper) per the spec's Testing list (partial, full-exact-rounding, over-refund-nothing-applied, idempotency, cash→cashMovements + no-open-shift throw, gift-card balance+txn, method guard, non-paid guard, owner-scope, P&L). Run → confirm FAIL.
- [ ] **Step 4: implement `convex/refunds.ts`**:
  - `create` mutation (args `{ orderId, clientId, cashierId, method, lines:[{lineIndex,qty}], reason? }`): idempotency via `by_cafe_clientId`; guards (paid-only, method∈tenders, cash⇒open shift); scan `refunds.by_order` → `alreadyRefundedQtyByIndex`; `validateRefundLines`; `fullyRefundsOrder ⇒ amountIDR = order.totalIDR − (order.refundedIDR ?? 0)`; assert `(refundedIDR ?? 0) + amountIDR ≤ totalIDR`; apply side-effects (inventory restock, pro-rated loyalty, gift-card, cash `cashMovements{out}`); insert `refunds` row; patch `order.refundedIDR`. Return `v.id('refunds')`.
  - `forOrder` internal helper or inline: compute `alreadyRefundedQtyByIndex`.
  - Proper Convex return validators.
- [ ] **Step 5: `orders.refundInfo` query** + expose `refundedIDR` on `getById` — `refundInfo({orderId})` returns `{ refundedIDR, fullyRefunded, methods, lines:[{lineIndex,nameSnapshot,qty,refundedQty,remainingQty,unitRefundIDR}] }`. Add `refundedIDR: v.optional(v.number())` to `getById`'s returns + handler.
- [ ] **Step 6: register + tests + commit** — api.d.ts (`refunds`); `pnpm test tests/convex/refunds.test.ts tests/convex/refund-math.test.ts` + full PASS; `pnpm typecheck` PASS. Commit:
  `git add convex/refunds.ts convex/lib/refund.ts convex/schema.ts convex/orders.ts convex/_generated/api.d.ts tests/convex/refunds.test.ts tests/convex/refund-math.test.ts && git commit -m "feat(orders): full + partial refunds — restock, loyalty, gift-card, cash drawer"`
  > Do NOT run codegen.

---

### Task 2: Backend — reporting integration (TDD)
**Files:** modify `convex/reports.ts` (`profitLoss`, `overview`), `convex/dashboard.ts`; extend `tests/convex/refunds.test.ts` or the reports test.

READ: `convex/reports.ts` `paidInRange`/`profitLoss`/`overview`; `convex/dashboard.ts` kpis; how a range resolves to `[startMs,endMs]`.

- [ ] **Step 1: FAILING report tests** — with revenue + a refund in range: `profitLoss` returns `refundsIDR`>0, `netRevenueIDR = revenueIDR − refundsIDR`, `cogsIDR` reduced by the refunded line's COGS, net profit down; `overview.revenueIDR` is net; `dashboard` kpi revenue net. Run → FAIL.
- [ ] **Step 2: implement** — in `profitLoss`: query `refunds.by_cafe_at` in `[startMs,endMs]` → `refundsIDR = Σ amountIDR`; `refundCogsIDR` = for each refund, for each refunded line use `order.lines[lineIndex].recipeSnapshot × qty × cost`; add `refundsIDR` to returns, `netRevenueIDR`, reduce `cogsIDR` by `refundCogsIDR`, recompute gross/net. In `overview` + `dashboard` kpis: subtract `refundsIDR` (in range) from `revenueIDR` (net) + expose `refundsIDR`.
- [ ] **Step 3: tests + typecheck + commit**
  `pnpm test` + `pnpm typecheck` PASS. Commit:
  `git add convex/reports.ts convex/dashboard.ts tests && git commit -m "feat(reports): net revenue + refund COGS in P&L, overview, dashboard"`

---

### Task 3: Frontend — refund dialog + receipt-preview wiring
**Files:** create `src/components/sale/refund-dialog.tsx`; modify `src/components/sale/receipt-preview.tsx`, `src/routes/_pos/reports/profit-loss.tsx`.

READ: `src/components/sale/receipt-preview.tsx` (the void button block ~line 221–240 + the AlertDialog + `can('canVoid')` + `cashierId` source + `useActiveCashier`), `src/components/sale/cash-payment-dialog.tsx` (a dialog with a method/qty form to mirror styling), `src/components/ui/{dialog,select,input,checkbox,button,badge}`, `~/lib/money`, `~/lib/toast`, the P&L `<dl>`/`lines`.

- [ ] **Step 1: `refund-dialog.tsx`** — props `{ orderId, open, onOpenChange, onDone }`. `useQuery(api.orders.refundInfo, orderId?{orderId}:'skip')`. Render each line: a `Checkbox` + name + a qty `Input`/stepper (default `remainingQty`, clamp `0..remainingQty`; disabled + "Sudah direfund" when `remainingQty===0`). A method `Select` (from `info.methods`, default the first). A reason `Input`. A live refund total (Σ checked `unitRefundIDR × qty`; show "Seluruh sisa" hint when all remaining selected). Submit → `useMutation(api.refunds.create)` with `clientId` (`crypto.randomUUID()`), `cashierId` from `useActiveCashier`, `lines` = checked rows with qty>0; disable while in-flight; toast success/error; `onOpenChange(false)` + `onDone()` on success. Guard submit when nothing selected.
- [ ] **Step 2: wire into `receipt-preview.tsx`** — add a "Refund" `Button` (variant outline) next to "Batalkan pesanan", shown when `order.paymentStatus === 'paid' && can('canVoid') && !(order.refundedIDR >= order.totalIDR)`; opens `<RefundDialog>`. Add a `Direfund {formatIDR(order.refundedIDR)}` badge near the status when `(order.refundedIDR ?? 0) > 0`. (`getById` now returns `refundedIDR`.)
- [ ] **Step 3: P&L line** — `profit-loss.tsx`: add a "− Pengembalian" row (`−{formatIDR(data.refundsIDR)}`) right after "Pendapatan" / before "= Laba kotor" (so gross profit reflects net revenue − net COGS), in both the `<dl>` and the CSV `lines`; include `refundsIDR` in the no-data empty check.
- [ ] **Step 4:** typecheck + test PASS. Commit:
  `git add src/components/sale/refund-dialog.tsx src/components/sale/receipt-preview.tsx src/routes/_pos/reports/profit-loss.tsx && git commit -m "feat(sale): refund dialog + receipt-preview wiring + P&L refunds line"`

---

### Task 4: i18n
New BI (see spec) — UI: `Refund`, `Pengembalian`, `Refund pesanan`, `Jumlah refund`, `Refund ke`, `Sudah direfund`, `Direfund {0}`, `Pilih item untuk direfund.`, `Seluruh sisa`, `− Pengembalian`; server-thrown messages stay off-catalog.
- [ ] `pnpm lingui:extract`; fill `en` (`Refund`, `Refund order`, `Refund amount`, `Refund to`, `Already refunded`, `Refunded {0}`, `Select items to refund.`, `All remaining`, `− Refunds`) + any other new empties; `pnpm lingui:compile` → en 0 missing. Commit `src/locales`.

---

### Task 5: Final verification + adversarial review
- [ ] `pnpm typecheck` PASS; `pnpm test` PASS; `pnpm lingui:compile` en 0 missing; `git status` clean (no route change → no routeTree).
- [ ] code-reviewer on `git diff main...HEAD` of `refunds.create` + `lib/refund.ts` + the report deltas: over-refund cap (cumulative `refundedIDR ≤ totalIDR`, per-line `qty ≤ remaining`, nothing applied on reject — validate-before-apply); exactly one inventory movement per refunded line with the right `+delta`; full-refund rounding lands `refundedIDR === totalIDR` exactly; loyalty pro-ration floors at 0 and can't go negative or double-credit; gift-card balance rises by exactly `amountIDR`; cash refund writes exactly one `cashMovements{out}` of `amountIDR` and requires an open shift; idempotency (same `clientId` → side effects once); method guard; non-paid guard; owner-scope; reports subtract refunds by `refund.at` in range (not double-counting; refund in a different period than the sale lands in the refund's period); refund COGS uses the order's `recipeSnapshot`. Address findings; re-verify.
- [ ] **Manual sanity:** a paid 3-line order → refund 1 of a line → stock up by that line's recipe, `refundedIDR` set, badge shows, P&L "− Pengembalian" appears, drawer (cash) down; refund the rest → fully refunded (button hides), cumulative = total exactly; over-refund blocked; QRIS refund logs but no drawer change.

---

## Self-Review
**Spec coverage:** refunds table + `orders.refundedIDR` (T1); pure helpers + `create` with all reversals + caps + idempotency + cash/open-shift guard (T1); `refundInfo`/`getById` (T1); P&L/overview/dashboard net + refund COGS (T2); dialog + receipt wiring + P&L line (T3); tests partial/full/over/idempotency/cash/giftcard/method/non-paid/scope/P&L (T1–T2); i18n (T4); adversarial review (T5). ✓
**Placeholder scan:** test seeding "copy from orders tests"; UI "mirror void block + cash-dialog". Else spec code.
**Type consistency:** `refunds.create({orderId,clientId,cashierId,method,lines:[{lineIndex,qty}],reason?})` ↔ dialog submit; `refundInfo` `{refundedIDR,fullyRefunded,methods,lines:[{lineIndex,nameSnapshot,qty,refundedQty,remainingQty,unitRefundIDR}]}` ↔ dialog; `getById.refundedIDR` ↔ receipt badge; `profitLoss.refundsIDR` ↔ P&L line. `unitRefundIDR`/`validateRefundLines` shared by mutation + tests. ✓
